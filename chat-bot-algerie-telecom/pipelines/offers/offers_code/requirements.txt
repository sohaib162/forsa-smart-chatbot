# Core dependencies for the 3-layer retrieval pipeline

# BM25 for sparse retrieval
rank-bm25>=0.2.2

# Sentence transformers for dense retrieval (multilingual embeddings)
sentence-transformers>=2.2.2

# Dependencies of sentence-transformers (usually auto-installed)
torch>=1.11.0
transformers>=4.19.0

# NumPy for numerical operations
numpy>=1.21.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: for better logging and formatting
colorlog>=6.7.0
# JSON and file processing
# (json, pathlib are built-in to Python)

# For command-line argument parsing
# (argparse is built-in to Python)

# For timing and performance measurement
# (time is built-in to Python)

# For collections and data structures
# (collections, defaultdict are built-in to Python)